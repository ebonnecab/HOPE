$(function () {
    const quizTemplateScript = $("#quiz-template").html();

    // Compile the template
    const quizTemplate = Handlebars.compile(quizTemplateScript);

    // Define data object
    let quiz = {
        
    }
})